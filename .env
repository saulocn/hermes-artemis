# Default Compose profile: keep `docker compose up` meaning "Artemis" (as before profiles existed).
# Override on the CLI or in your shell to switch, e.g.: COMPOSE_PROFILES=rabbit docker compose up
COMPOSE_PROFILES=artemis
